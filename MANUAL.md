# 📘 전인교육학회 뉴스레터 스튜디오 사용 매뉴얼

> 인사말 + 5개 아티클로 구성된 뉴스레터를 작성·미리보기·발송까지 한 곳에서 처리하는 도구입니다.

---

## 1. 화면 구성

| 영역 | 위치 | 설명 |
|---|---|---|
| **상단 바** | 화면 위 | 현재 초안 이름 · 템플릿 전환(Classic/Magazine/Minimal) · 다크모드 · HTML 내보내기 · 다른 이름으로 저장 · 저장 |
| **좌측 사이드바** | 화면 왼쪽 (64px) | 편집 / 초안 / 발송 / 매뉴얼 4개 탭 (아이콘) |
| **편집 패널** | 가운데 | 입력 폼 (인사말, 5개 아티클, 푸터). 편집 탭에서만 접기 가능 |
| **미리보기** | 오른쪽 | 실시간 렌더링. PC / 모바일 / 동시 보기 전환 |

> 💡 **대표 초안** 기능: 초안 탭에서 별(★) 버튼으로 대표 초안을 지정하면, 새로고침 시 해당 초안이 자동으로 로드됩니다.

---

## 2. 기본 사용 흐름

1. **편집** 탭에서 호수, 발행일, 홈페이지 URL, 수신동의 URL, 인사말, 아티클 5개, 푸터를 입력
2. 우측 미리보기는 입력 즉시 자동 업데이트 (300ms debounce)
3. 상단 **템플릿 스위처**로 Classic / Magazine / Minimal 디자인을 즉시 전환
4. 작업 중 언제든 **저장** 또는 **다른 이름으로 저장**
5. **발송** 탭에서 발송 방식과 수신자를 선택해 메일 발송

---

## 3. 핵심 기능

### 🖼 이미지 업로드 (Cloudinary 자동 호스팅)

세 가지 방법으로 이미지를 추가할 수 있습니다:

1. **드롭존 클릭** → 파일 선택 다이얼로그
2. **드래그 앤 드롭** → 편집 패널의 점선 박스에 파일을 놓기
3. **미리보기에 직접 드롭** → 우측 미리보기 화면의 이미지 위로 끌어다 놓으면 즉시 교체 (드롭 가능한 이미지에는 녹색 아웃라인 표시)

업로드된 이미지는 자동으로 Cloudinary에 호스팅되어 메일에서 안정적으로 표시됩니다.

#### 📐 권장 사이즈

| 용도 | 권장 업로드 크기 | 비율 |
|---|---|---|
| 인사말 사진 | 160×160 | 1:1 (정사각) |
| Hero / 큰 배너 | 1080×720 | 3:2 |
| 좌·우 이미지 카드 | 360×260 | 4:3 |
| 세로형 카드 (학회지 표지 등) | 280×380 | 3:4 |
| Magazine 그리드 | 500×320 | 3:2 |
| Minimal 썸네일 | 200×200 | 1:1 |

각 파일은 **300KB 이하**로 압축하면 메일 로딩이 빨라집니다.

> 모바일 미리보기에서 5번째 아티클(세로 이미지)은 자동으로 폭의 50%로 줄어들고 양쪽 흰색 여백이 생깁니다.

### 🖼 이미지 클릭 시 링크 연결

뉴스레터의 모든 아티클 이미지를 클릭하면 해당 아티클의 링크로 이동합니다. 제목뿐 아니라 이미지도 클릭 가능한 링크로 동작합니다.

### ✏️ 미리보기에서 직접 편집

미리보기 화면에서 **거의 모든 텍스트를 클릭하면 바로 편집**할 수 있습니다:

- 이름 / 직함 / 인사말 본문 / 펼쳐보기 내용 / 서명
- 아티클의 카테고리 뱃지 / 제목 / 요약 / 메타
- 푸터의 주소 / 연락처

다른 곳을 클릭(또는 단일 라인 필드는 Enter)하면 자동 저장됩니다.

#### 🔗 미리보기 링크

- **한 번 클릭** → 텍스트 편집 (링크 무시)
- **더블 클릭** → 새 탭에서 링크 열기

### 📱 PC / 모바일 / 동시 보기

미리보기 우상단의 3개 토글 버튼:

| 버튼 | 동작 |
|---|---|
| ▦ 동시 | PC + 모바일을 좌우로 동시에 표시 (기본) |
| 🖥 PC만 | 760px Desktop 미리보기만 |
| 📱 모바일만 | 390px Mobile 미리보기만 |

### 👤 인사말 사진 토글

인사말 섹션에서 **"프로필 사진 사용"** 체크박스로 사진 표시 여부를 선택할 수 있습니다.

- **체크 ON** (기본): 프로필 사진이 이름/직함 옆에 표시됨
- **체크 OFF**: 사진 없이 이름/직함/본문만 표시. 레이아웃이 자연스럽게 재배치됨

> Minimal 템플릿은 원래 사진 없는 텍스트 중심 디자인이므로 영향 없음.

### 📂 펼쳐보기 (긴 인사말 처리)

인사말 본문이 길 경우 두 입력칸으로 분리해서 입력합니다:

- **본문**: 항상 표시되는 부분
- **펼쳐보기 내용**: "펼쳐보기 ▾" 버튼을 눌러야 보이는 부분

> Apple Mail / iOS Mail 등은 클릭으로 펼치고 접을 수 있고, Gmail / Outlook에서는 자동으로 모두 표시됩니다. 내용 손실은 없습니다.

### ↩️ 줄바꿈 처리

본문/요약 입력란에서 그냥 **엔터키로 줄바꿈**하면 메일에서도 동일하게 줄바꿈됩니다. `<br>` 같은 HTML 태그를 직접 입력할 필요가 없습니다.

### 🔄 아티클 순서 드래그 변경

아티클 카드 좌상단의 **⋮⋮ grip 핸들**을 잡고 다른 카드 위로 끌면:

- 대상 카드 위에 녹색 인디케이터 라인과 **"여기로 이동"** 라벨이 표시됨
- 드롭하면 순서가 바뀜
- **Classic 템플릿에서는 레이아웃도 함께 이동** — 1번 위치(hero)의 아티클을 3번으로 옮기면 hero 레이아웃 자체가 3번으로 이동
- Magazine, Minimal은 콘텐츠만 이동하고 레이아웃은 위치에 종속

### 💾 두 가지 저장 방식

| 버튼 | 동작 |
|---|---|
| **저장** | 현재 작업 중인 초안에 **덮어쓰기**. 새 작업이면 모달이 떠서 이름을 묻고 새 파일로 저장. 성공 시 "✓ 저장됨" 토스트 |
| **다른 이름으로 저장** | 항상 모달을 띄워 **새 파일**로 저장. 같은 이름이 이미 있으면 `(2)`, `(3)` 자동 부여 |

### 📁 초안 관리

**초안** 탭에서:

#### 대표 초안
- 각 초안 옆의 **별(☆) 버튼**을 클릭하여 대표 초안으로 지정
- 대표 초안으로 지정된 초안은 별이 채워진 상태(★)로 표시
- 새로고침 시 대표 초안이 자동으로 로드됨
- 다시 클릭하면 해제

#### 초안 목록
- **불러오기**: 클릭으로 편집 탭에서 이어 작업
- **삭제**: 삭제 버튼 클릭 (대표 초안을 삭제하면 자동 해제)
- **이름 변경**: 초안 이름 **더블클릭** → 인라인 입력 → Enter로 저장 (현재 작업 중인 초안이면 상단바 이름도 자동 동기화)

### 🌓 다크모드

상단 바의 **달/해 아이콘**으로 라이트/다크 테마를 전환할 수 있습니다. 설정은 브라우저에 자동 저장됩니다.

### 🗂 편집 패널 접기

편집 탭의 패널 헤더 우상단 « 버튼으로 편집 패널을 접을 수 있습니다. 접힌 상태에서는 미리보기 좌측에 흰색 핸들이 표시되고, 클릭하면 다시 펼쳐집니다.

### 🌐 뉴스레터 헤더 링크

| 요소 | 연결 대상 |
|---|---|
| **학회명** (최상단) | 홈페이지 URL (기본 정보에서 설정) |
| **"학회의 더 자세한 소식을 받아보세요 →"** | 수신동의 URL (기본 정보에서 설정) |
| **하단 수신동의** (푸터) | 수신동의 URL |
| **하단 수신거부** (푸터) | 자동 생성된 수신거부 링크 |

---

## 4. 수신동의 페이지

뉴스레터에서 "학회의 더 자세한 소식을 받아보세요" 또는 하단 "수신동의" 링크를 클릭하면 **수신동의 폼 페이지**로 이동합니다.

### 수신동의 폼

방문자가 아래 정보를 입력하고 동의하면 구독자로 등록됩니다:

| 필드 | 필수 여부 | 설명 |
|---|---|---|
| **이름** | 필수 | 구독자 이름 |
| **이메일** | 필수 | 뉴스레터 수신 이메일 |
| **소속** | 선택 | 대학교/기관명 |
| **개인정보 동의** | 필수 | 수집·이용 동의 체크 |

### 수신동의 페이지 URL

```
http://<서버주소>/api/subscribe
```

발송 탭의 **"수신동의 URL 복사"** 버튼으로 복사할 수 있습니다.

### 구독자 관리 (발송 탭)

발송 탭의 **"수신동의 명단 (캠프 홍보 등)"** 섹션에서:

- **구독자 목록**: 등록된 구독자의 이름, 소속, 이메일, 등록일 확인 및 삭제
- **수신자로 불러오기**: 구독자 명단을 수신자 명단에 바로 로드 (CSV 업로드 없이 즉시 발송 가능)
- **CSV 내보내기**: 구독자 목록을 `subscribers.csv` 파일로 다운로드
- **수신동의 URL 복사**: 수신동의 페이지 주소를 클립보드에 복사

> 수신동의로 등록된 구독자 중 수신거부한 사람은 발송 시 자동으로 제외됩니다.

---

## 5. 발송 가이드

### 발송 방식

| 방식 | 용도 | 한도 |
|---|---|---|
| **MailHog** | 로컬 테스트 (실제 발송 X — http://localhost:8025 에서 확인) | 무제한 |
| **Gmail SMTP** | 본인 Gmail로 소량 발송 | 일 500건 |
| **NCP Cloud Outbound Mailer** | **국내 대량 발송 권장** (10만 명도 안전 처리) | 사용량제 (한도 증액 가능) |

### ⏱ 발송 소요 시간 (예상)

NCP는 **500명/요청 + 청크 사이 5초 sleep + 자동 재시도** 방식으로 안전하게 처리합니다.

| 인원 | 청크 수 | 예상 시간 | 비고 |
|---|---|---|---|
| 100명 | 1 | ~3초 | 즉시 완료 |
| 1,000명 | 2 | ~10초 | |
| 5,000명 | 10 | ~1분 | |
| 10,000명 | 20 | ~2분 | |
| 30,000명 | 60 | ~6분 | |
| 50,000명 | 100 | ~10분 | |
| **100,000명** | **200** | **~20~30분** | NCP 한도 증액 필요 |

> **MailHog / Gmail**은 1명씩 직렬 발송이라 느립니다. Gmail의 경우 1,000명 발송에 약 10~20분 소요. 1,000명 이상은 **반드시 NCP 사용 권장.**

### 백그라운드 발송 + 실시간 진행률

발송 버튼을 누르면 작업이 **백그라운드에서 실행**되고 화면에는 진행률이 실시간 표시됩니다:

- 진행률 바 (0~100%)
- 현재 청크 번호 / 전체 청크 수
- 경과 시간 (초)
- 누적 성공/실패 카운트

발송 중에는 버튼이 비활성화되며 **"발송 중... 12,345/100,000"** 으로 표시됩니다.

> **발송 중에는 백엔드 컨테이너를 재시작하지 마세요.** 진행 상황은 메모리에 저장되므로 재시작 시 사라집니다 (이미 발송된 메일은 도달하지만 진행 카운터는 리셋).

### 수신자 등록 방법

수신자를 등록하는 두 가지 방법이 있습니다:

#### 방법 1: CSV/Excel 파일 업로드
컬럼: `email` 또는 `email,name` · CSV / Excel(.xlsx) 지원

**단일 컬럼:**
```
email
hong@example.com
kim@example.com
```

**이름 포함:**
```
email,name
hong@example.com,홍길동
kim@example.com,김철수
```

#### 방법 2: 수신동의 명단에서 불러오기
수신동의 페이지를 통해 등록된 구독자를 **"수신자로 불러오기"** 버튼 한 번으로 수신자 명단에 로드할 수 있습니다. CSV 업로드 없이 즉시 발송 준비가 완료됩니다.

### 발송 절차

1. **발송** 탭 → 발송 방식 선택
2. 발신자 이메일 / 이름 입력
3. 수신자 등록 (CSV 업로드 또는 수신동의 명단에서 불러오기)
4. **테스트 발송** 버튼으로 본인 메일에 먼저 시험 발송 (강력 권장)
5. 결과 확인 후 **전체 발송** 클릭
6. 하단 콘솔에서 `✓ N건 성공 / N건 실패` 확인

### Gmail SMTP 설정

1. https://myaccount.google.com/security 에서 **2단계 인증** 켜기
2. https://myaccount.google.com/apppasswords 에서 **앱 비밀번호** 발급 (16자, 공백 제거)
3. `.env` 파일 수정:
   ```
   GMAIL_USER=youraccount@gmail.com
   GMAIL_APP_PASSWORD=abcdefghijklmnop
   ```
4. `docker compose restart backend`

### NCP Cloud Outbound Mailer 설정

1. https://www.ncloud.com 가입 → 결제수단 등록
2. 콘솔 → **Cloud Outbound Mailer** 신청
3. **발신 도메인 등록** + DNS에 SPF/DKIM TXT 레코드 추가 → 인증 완료
4. **발신 주소록**에 발신 이메일 등록 + 인증 메일 클릭
5. 마이페이지 → **인증키 관리** → Access Key / Secret Key 발급
6. **(대량 발송 시 필수)** NCP 콘솔에서 일일 발송 한도 증액 신청 (영업일 1~2일 소요)
   - 1만 명 미만 → 기본 한도로 충분
   - 1만 명 이상 → 한도 증액 필수
7. `.env` 파일 수정:
   ```
   NCP_ACCESS_KEY=ncp_iam_xxxxxxxxxx
   NCP_SECRET_KEY=xxxxxxxxxxxxxxxx
   NCP_SENDER_ADDRESS=noreply@humancompletion.org
   NCP_SENDER_NAME=전인교육학회
   PUBLIC_BASE_URL=https://api.humancompletion.org   # 외부 접근 가능 도메인 (수신거부 링크용)
   ```
8. `docker compose restart backend`

### 🔒 NCP 안전 발송 메커니즘

- **청크 분할**: 한 API 요청에 최대 500명 (NCP 한도 1,000명의 절반으로 안전 마진)
- **청크 간격**: 5초 sleep (TPS 보호 + ISP 평판 보호)
- **자동 재시도**: 429(rate limit) / 5xx 에러 시 10초 → 20초 → 30초 간격으로 최대 3회 재시도
- **부분 실패 허용**: 한 청크가 실패해도 다음 청크 계속 진행
- **개인화**: 각 수신자에게 다른 수신거부 링크가 NCP 변수 치환으로 자동 적용

---

## 6. 단축키 & 인터랙션

| 키 / 동작 | 결과 |
|---|---|
| `Enter` (저장 모달) | 초안 저장 |
| `Esc` (저장 모달) | 취소 |
| 미리보기 텍스트 단일 클릭 | 인라인 편집 시작 |
| 미리보기 링크 더블 클릭 | 새 탭에서 링크 열기 |
| 다른 곳 클릭 (인라인 편집) | 편집 종료 + 자동 저장 |
| 본문 텍스트박스 Enter | 줄바꿈 (자동으로 메일 줄바꿈으로 변환) |
| 초안 이름 더블 클릭 | 인라인 이름 변경 모드 |
| 초안 별(☆) 클릭 | 대표 초안 지정/해제 |
| 아티클 카드 ⋮⋮ 핸들 드래그 | 순서 변경 |
| 미리보기 이미지에 파일 드롭 | 이미지 즉시 교체 |

---

## 7. 자주 묻는 질문

**Q. 저장한 초안은 어디에 보관되나요?**
프로젝트 폴더의 `drafts/` 디렉토리에 JSON 파일로 저장됩니다. 컨테이너를 내려도 사라지지 않으며, 폴더를 다른 위치로 옮긴 뒤 다시 시작해도 그대로 불러옵니다.

**Q. 같은 이름으로 저장하면 덮어쓰나요?**
- **저장** 버튼: 현재 작업 중인 초안에 덮어쓰기
- **다른 이름으로 저장**: 항상 새 파일. 같은 이름이 있으면 `(2)`, `(3)` 자동 부여

**Q. 새로고침할 때 특정 초안이 자동으로 열리게 하려면?**
초안 탭에서 원하는 초안의 별(☆) 버튼을 클릭하여 **대표 초안**으로 지정하세요. 이후 새로고침 시 항상 해당 초안이 자동으로 로드됩니다.

**Q. 이미지가 메일에서 깨져 보여요.**
이메일 클라이언트는 로컬 이미지를 차단합니다. **반드시 Cloudinary 업로드 기능**을 통해 외부 URL로 업로드된 이미지를 사용해야 합니다.

**Q. Outlook에서 디자인이 깨져요.**
이메일 표준의 한계입니다. 둥근 모서리(`border-radius`), 일부 그라디언트, `<details>` 토글 등은 Outlook에서 무시되어 단순한 형태로 표시됩니다 — 내용은 모두 보이므로 정상입니다.

**Q. 미리보기에서 텍스트를 수정했는데 사이드바에 반영이 안 돼요.**
편집을 마치고 **다른 곳을 클릭**하면 자동으로 동기화됩니다.

**Q. 다른 컴퓨터에서 이어서 작업하고 싶어요.**
프로젝트 폴더 전체(특히 `drafts/`, `recipients/`, `.env`)를 옮기거나, Git에 push 해두면 됩니다. (`.env`는 `.gitignore`에 의해 자동 제외됨)

**Q. 수신동의 페이지는 어떻게 공유하나요?**
발송 탭의 **"수신동의 URL 복사"** 버튼을 클릭하면 수신동의 페이지 주소가 복사됩니다. 이 URL을 캠프 홍보, 학술대회 안내 등에 활용하세요.

**Q. 수신동의로 등록된 구독자에게 바로 발송할 수 있나요?**
네. 발송 탭에서 **"수신자로 불러오기"** 버튼을 클릭하면 구독자 명단이 수신자로 즉시 로드됩니다. CSV 파일을 따로 준비할 필요가 없습니다.

**Q. Magazine과 Minimal에서도 아티클 드래그로 레이아웃이 함께 이동하나요?**
아니요. **Classic**에서만 레이아웃이 아티클을 따라갑니다. Magazine·Minimal은 위치마다 시각 구조(첫 기사 hero, 그리드, 마지막 tall 등)가 강하게 결정되어 있어 콘텐츠만 이동합니다.

---

## 8. 문제 해결

| 증상 | 원인 | 해결 |
|---|---|---|
| 이미지 업로드 실패 | Cloudinary 키 미설정 | `.env`의 `CLOUDINARY_*` 3개 키 확인 후 `docker compose restart backend` |
| 미리보기 갱신 안 됨 | Vite HMR 캐시 | `Cmd + Shift + R` 하드 리로드 |
| 메일 발송 실패 | 발송 서비스 키 누락 | `.env`의 발송 서비스 키 확인 |
| Gmail "Username and Password not accepted" | 일반 비밀번호 입력 | 반드시 **앱 비밀번호** 사용 |
| NCP 403 Forbidden | API 키 잘못 입력 | Access/Secret Key 다시 확인, 공백 제거 |
| NCP 발신 주소 미인증 | 발신 주소록 미등록/미인증 | 발신 주소록 인증 메일 확인 |
| 메일이 스팸함으로 감 | 도메인 SPF/DKIM 미설정 | NCP 발신 도메인 인증 완료 확인 |
| 컨테이너 충돌 | 이전 컨테이너 잔존 | `docker compose down` 후 다시 `up` |

---

## 9. 데이터 백업

뉴스레터 작업물 백업은 아래 폴더만 보관하면 됩니다:

```
drafts/        ← 저장된 초안 JSON
recipients/    ← 수신자 CSV, 수신거부 목록, 구독자 목록
.env           ← API 키 (절대 공개 금지)
```

Git 사용 시 `.env`는 자동으로 제외되며(`.gitignore`), 키는 안전하게 보호됩니다.

---

## 10. 발송 전 체크리스트

- [ ] 편집 탭에서 PC / 모바일 미리보기 모두 확인
- [ ] 이미지가 모두 Cloudinary URL인지 확인
- [ ] 링크가 올바른 URL을 가리키는지 더블클릭으로 확인
- [ ] 홈페이지 URL, 수신동의 URL이 올바르게 설정되어 있는지 확인
- [ ] **테스트 발송**으로 본인 메일에 먼저 시험 발송
- [ ] 받은 메일에서 이미지 / 줄바꿈 / 펼쳐보기 / 이미지 링크 동작 확인
- [ ] 수신자 명단 인원 수 확인 (CSV 업로드 또는 수신동의 명단에서 불러오기)
- [ ] 제목, 발신자, 발신 이메일 최종 확인
- [ ] **전체 발송** 실행

---

문의 · 학회 사무국 · info@humancompletion.org
